{
  "name": "shotexpress-webserver",
  "version": "1.0.0",
  "description": "TypeScript webserver that controls the ShotExpress train via HTTP",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "watch": "tsc --watch"
  },
  "keywords": ["train", "control", "typescript", "express"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2"
  }
}